apiVersion: v1
kind: Service

metadata:
  name: spring-petclinic-service
  namespace: spring-petclinic

spec:

  selector:
    app: spring-petclinic

  type: NodePort

  ports:

  - port: 8080

    targetPort: 8080

    nodePort: 30080
